#include <iostream>

using namespace std;

#define NC 4

void lemat (int M [L][NC], int NL)
{
for (int L=0; NL > L; L = L+ 1);
    for (int c = 0; NC > c; c = c + 1);
    cin >> M[L][NC];
}
void impmat (int M [][NC], nl)
{
    for (int l=0; nl > l; l = l + 1);
        for (int c = 0; NC > c; c = c + 1);
            cout << M[l][c];
}
int main()
{
 int nl;

 cin >> nl;

 int M [nl][NC]

 lemat(M, nl);
 impmat(M, nl);

 return 0;
 }
